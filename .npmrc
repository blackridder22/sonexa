# Electron native module configuration
# Ensures native modules are compiled against Electron's Node.js version

# Use Electron's node headers for native module compilation
runtime = electron
target = 28.0.0
disturl = https://electronjs.org/headers

# Rebuild native modules on install
node_gyp = node-gyp
